/* Mode Easton v33 neutralisé temporairement pour rétablir la stabilité TEST.
   Aucun chargement v35, aucun observer, aucune boucle de fond.
   La logique Easton dédiée sera reconstruite proprement après validation de la stabilité. */
(() => {
  'use strict';
  window.AssistantArcherEastonModeDisabled = true;
})();
